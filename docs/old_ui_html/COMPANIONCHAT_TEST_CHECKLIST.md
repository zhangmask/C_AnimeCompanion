# CompanionChat 开发验收清单

> 依据 `COMPANIONCHAT_DESIGN.md` 设计文档，按开发顺序排列。
> 每个验收项格式：`[ ] 编号 | 验收内容 | 验证方式`
> 层级标记：`[后端]` = 数据/逻辑层 | `[前端]` = UI/交互层 | `[集成]` = 前后端联调

---

## 阶段零：Room 基础设施

> 目标：引入 Room 依赖，创建数据库骨架，不破坏现有功能。

### 0.1 依赖与编译

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 0.1.1 | 后端 | `build.gradle.kts` 中正确添加 Room runtime、ktx、compiler 依赖 | `gradlew :app:assembleDebug` 编译通过，无依赖冲突 |
| 0.1.2 | 后端 | Room compiler 使用 KSP 而非 kapt（或与项目现有注解处理器一致） | 编译无 warning，生成代码在 `build/generated/ksp` 下 |
| 0.1.3 | 后端 | 创建 `CompanionDatabase` 类，继承 `RoomDatabase`，包含所有 Entity 声明 | 类编译通过，`@Database` 注解的 entities 数组完整 |

### 0.2 数据库表定义

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 0.2.1 | 后端 | `ConversationEntity` 表结构正确：id(String)、title、createdAt、updatedAt | Room 编译无 Entity 注解报错 |
| 0.2.2 | 后端 | `MessageEntity` 表结构正确：id(String)、conversationId(FK逻辑关联)、role(String)、content、timestamp | 同上 |
| 0.2.3 | 后端 | `Memory` 表结构正确：id(Long自增)、content、category、layer、source、referenceCount、sessionId、createdAt、updatedAt、expiresAt | 同上 |
| 0.2.4 | 后端 | `memories_fts` FTS5 虚拟表通过 `@Query` 的 `CREATE VIRTUAL TABLE` 正确创建 | 数据库初始化后 `SELECT * FROM memories_fts` 不报错 |
| 0.2.5 | 后端 | FTS5 的 INSERT/DELETE/UPDATE 三个触发器正确创建 | 对 memories 表做增删改后，memories_fts 同步变化 |
| 0.2.6 | 后端 | `UserPreference` 表结构正确：id(Long自增)、category、content、confidence、createdAt、updatedAt | Room 编译无报错 |
| 0.2.7 | 后端 | `Skill` 表结构正确：id(Long自增)、name、description、systemPrompt、icon、isBuiltIn、isActive、usageCount、createdAt、updatedAt | Room 编译无报错 |

### 0.3 DAO 接口

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 0.3.1 | 后端 | `ConversationDao`：增删改查 + 按时间倒序查询全部会话 | 单元测试：插入3条会话，查询返回3条且顺序正确 |
| 0.3.2 | 后端 | `MessageDao`：按 conversationId 查询消息列表 + 按时间正序 | 单元测试：插入5条消息到同一会话，查询返回5条且时间正序 |
| 0.3.3 | 后端 | `MemoryDao.searchByFTS`：FTS5 MATCH 查询返回匹配结果 | 单元测试：插入"用户喜欢编程"记忆，搜索"编程"命中 |
| 0.3.4 | 后端 | `MemoryDao.incrementReference`：引用计数 +1 | 单元测试：插入记忆 referenceCount=0，调用后变1 |
| 0.3.5 | 后端 | `MemoryDao.promoteToLongTerm`：layer 从 short_term 变 long_term | 单元测试：插入短期记忆，调用后查询 layer=long_term |
| 0.3.6 | 后端 | `MemoryDao.cleanupExpiredShortTerm`：只删除过期的短期记忆 | 单元测试：插入2条过期短期记忆 + 1条未过期 + 1条长期记忆，清理后只剩2条 |
| 0.3.7 | 后端 | `MemoryDao.getPromotableShortTerm`：返回 referenceCount ≥ 3 的短期记忆 | 单元测试：插入 referenceCount=3 的短期记忆，查询命中 |
| 0.3.8 | 后端 | `PreferenceDao`：按 category 查询 + 去重合并（同 category+相似 content → confidence+1） | 单元测试：重复插入同类偏好，验证 confidence 递增 |
| 0.3.9 | 后端 | `PreferenceDao.getConfirmed`：只返回 confidence ≥ 3 的偏好 | 单元测试：插入 confidence=1/2/3/5 各一条，查询返回2条 |
| 0.3.10 | 后端 | `SkillDao`：activate 方法正确设置 isActive=true 并 deactivate 其他 | 单元测试：激活技能A后，技能B自动取消激活 |
| 0.3.11 | 后端 | `SkillDao`：内置技能不可通过 DAO 删除（在业务层校验） | 单元测试：尝试删除 isBuiltIn=true 的技能，抛异常或被拦截 |

### 0.4 数据库初始化

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 0.4.1 | 后端 | `CompanionDatabase` 使用单例模式（`companion object` + `volatile`） | 多次调用 getInstance 返回同一实例 |
| 0.4.2 | 后端 | 内置技能通过 `RoomDatabase.Callback.onCreate` 预填充 | 首次安装后查询 skills 表有4条内置技能 |
| 0.4.3 | 后端 | 数据库版本号为1，无 migration（首次安装） | 全新安装后数据库正常创建，无崩溃 |

---

## 阶段一：会话存储迁移

> 目标：将 conversations.json 的数据迁移到 Room，所有读写走 Room，JSON 文件备份。

### 1.1 迁移逻辑

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 1.1.1 | 后端 | `DataMigration` 类：检测 `conversations.json` 是否存在 | 有文件返回 true，无文件返回 false |
| 1.1.2 | 后端 | 正确解析现有 JSON 格式（`SessionData`/`ChatSession` 结构） | 提供测试 JSON 文件，解析出的会话数和消息数与文件一致 |
| 1.1.3 | 后端 | 会话数据写入 `conversations` 表，消息数据写入 `messages` 表 | 迁移后 Room 中会话数 = JSON 中会话数，消息总数一致 |
| 1.1.4 | 后端 | 迁移成功后 `conversations.json` 重命名为 `conversations.json.bak` | 文件系统检查：.json 不存在，.json.bak 存在 |
| 1.1.5 | 后端 | 迁移失败时不删除原文件，不崩溃，打日志 | 模拟 JSON 解析异常，原文件仍在，应用正常启动（空数据） |
| 1.1.6 | 后端 | 迁移只执行一次（.bak 文件存在时跳过） | 第二次启动不重复迁移，日志显示"已迁移，跳过" |

### 1.2 ViewModel 适配

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 1.2.1 | 后端 | `ChatViewModel` 中 `loadSessions()` 改为从 Room 读取 | 启动后会话列表与数据库一致 |
| 1.2.2 | 后端 | `saveSessions()` 改为写入 Room（或移除，改为即时写入） | 发消息后 Room messages 表新增一条 |
| 1.2.3 | 后端 | 新建会话 → Room conversations 表新增一条 | UI 新建会话后数据库查询到新记录 |
| 1.2.4 | 后端 | 删除会话 → Room conversations + 关联 messages 一起删除 | 删除会话后 messages 表中该会话的消息也消失 |
| 1.2.5 | 后端 | 发送消息 → Room messages 表即时写入（不再等退出时批量保存） | 发10条消息后立即查数据库有10条 |

### 1.3 前端无感知验证

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 1.3.1 | 集成 | 首页会话列表正常显示（标题、消息数、时间） | 有旧数据时列表内容与迁移前一致 |
| 1.3.2 | 集成 | 点击会话能正常加载历史消息 | 进入旧会话，消息列表完整显示 |
| 1.3.3 | 集成 | 新建会话、发消息、切换会话、删除会话功能正常 | 操作一轮后数据持久化，杀进程重启数据仍在 |
| 1.3.4 | 集成 | 空数据首次启动：无崩溃，显示空状态引导 | 全新安装或清除数据后启动正常 |

---

## 阶段二：上下文管理

> 目标：长对话不超限，滑动窗口截断 + 历史摘要注入。

### 2.1 ContextManager 核心逻辑

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 2.1.1 | 后端 | `shouldCompress()`：消息数 ≤ N*2+10 时返回 false | 传入20条消息（N=10, 阈值30），返回 false |
| 2.1.2 | 后端 | `shouldCompress()`：消息数 > N*2+10 时返回 true | 传入35条消息，返回 true |
| 2.1.3 | 后端 | `buildContext()`：返回的 `ContextWindow.recentMessages` 恰好是最近 N 轮 | 传入50条消息 + N=10，返回最近20条（10轮×2） |
| 2.1.4 | 后端 | `buildContext()`：`historySummary` 在无摘要时为空字符串 | 首次压缩前 historySummary = "" |
| 2.1.5 | 后端 | `buildContext()`：`systemPrompt` 正确拼接基础 prompt + 偏好 + 摘要 | 三部分都有内容时，输出包含全部三段 |
| 2.1.6 | 后端 | N 值可配置（默认10），修改后 `shouldCompress` 阈值随之变化 | 设 N=5，消息数12条时 shouldCompress=true |

### 2.2 Conversation 重建

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 2.2.1 | 后端 | 压缩触发后，旧 `Conversation` 对象被 `close()` 释放 | 无内存泄漏日志，旧 conversation 不再被引用 |
| 2.2.2 | 后端 | 新 `Conversation` 创建时 system prompt 包含摘要 | 通过日志或回调检查 system prompt 内容 |
| 2.2.3 | 后端 | 最近 N 轮消息正确回放到新 Conversation | 回放后发一条消息，模型能引用回放内容中的信息 |
| 2.2.4 | 后端 | **降级方案**：如果 Conversation 不支持回放，system prompt 中注入完整最近对话摘要 | 模拟回放失败，降级后模型仍能理解上下文 |
| 2.2.5 | 后端 | 重建过程中用户发消息不会崩溃（锁/状态保护） | 在重建过程中并发发消息，应用不 ANR 不崩溃 |

### 2.3 历史摘要生成

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 2.3.1 | 后端 | 使用 Engine-B 对被丢弃的消息生成 ≤ 200 字的摘要 | 输入10条被丢弃消息，输出摘要长度 ≤ 200字 |
| 2.3.2 | 后端 | Engine-B 不可用时（未初始化/OOM），摘要为空字符串，不崩溃 | 模拟 Engine-B 不可用，compressHistory 返回 "" |
| 2.3.3 | 后端 | 摘要生成超时 60 秒后中断，返回空摘要 | 模拟超时，60秒后返回 ""，不阻塞主线程 |

### 2.4 前端表现

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 2.4.1 | 集成 | 连续发 30+ 条消息，应用不崩溃、不 ANR | 自动化脚本快速发30条消息，观察稳定性 |
| 2.4.2 | 集成 | 压缩后继续对话，模型能理解上下文（引用之前聊过的内容） | 手动测试：聊10轮→触发压缩→问"我们刚才聊了什么" |
| 2.4.3 | 集成 | 压缩过程中 UI 不卡顿（推理在协程中） | 压缩期间 UI 可正常滑动和操作 |
| 2.4.4 | 集成 | 用户可在设置中调整保留轮数 N | 修改 N=5 后，更早触发压缩 |

---

## 阶段三：记忆系统

> 目标：记忆写入、FTS5 检索、分层生命周期、记忆管理 UI。

### 3.1 记忆写入——规则提取器

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 3.1.1 | 后端 | "记住我叫小明" → 提取出 fact 类别记忆 "用户叫小明" | 单元测试验证正则匹配和提取结果 |
| 3.1.2 | 后端 | "我喜欢吃火锅" → 提取出 preference 类别 "用户喜欢吃火锅" | 同上 |
| 3.1.3 | 后端 | "我住在北京" → 提取出 fact 类别 "用户住在北京" | 同上 |
| 3.1.4 | 后端 | "不要再说这个了" → 提取出 preference 类别 "不要再说这个" | 同上 |
| 3.1.5 | 后端 | 普通消息（不含关键词）不触发提取 | "今天天气不错" → 不产生记忆 |
| 3.1.6 | 后端 | 提取的记忆自动写入 Room memories 表，layer=short_term | 数据库查询验证 |
| 3.1.7 | 后端 | 短期记忆的 expiresAt = 当前时间 + 7天 | 写入后检查 expiresAt 字段值 |

### 3.2 FTS5 检索

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 3.2.1 | 后端 | 插入记忆后 FTS5 虚拟表同步更新 | 插入"用户是Android开发者"后，FTS搜索"Android"命中 |
| 3.2.2 | 后端 | 删除记忆后 FTS5 虚拟表同步删除 | 删除后搜索不再命中 |
| 3.2.3 | 后端 | 更新记忆后 FTS5 同步更新 | 修改内容后搜索新内容命中，旧内容不命中 |
| 3.2.4 | 后端 | `retrieveRelevantMemories`：用户消息分词 → FTS5 搜索 → 返回相关记忆 | 消息"帮我写个Android应用" → 搜索命中"Android开发者"记忆 |
| 3.2.5 | 后端 | 检索结果优先排序：长期记忆 > 短期记忆（按 referenceCount） | 同时有短期和长期匹配时，长期排前面 |
| 3.2.6 | 后端 | 检索后 referenceCount 自动 +1 | 搜索命中后查数据库 referenceCount 已递增 |
| 3.2.7 | 后端 | 空消息或太短的消息（<2字关键词）不触发检索 | 消息"？"→ 返回空列表 |
| 3.2.8 | 后端 | 检索结果最多返回 5 条 | 插入10条匹配记忆，查询只返回5条 |

### 3.3 记忆分层生命周期

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 3.3.1 | 后端 | 应用启动时执行 `cleanupExpiredShortTerm`：过期短期记忆被删除 | 插入过期短期记忆，重启应用后消失 |
| 3.3.2 | 后端 | 应用启动时执行 `promoteCheck`：referenceCount ≥ 3 的短期记忆升级为长期 | 插入短期记忆 refCount=3，重启后 layer=long_term |
| 3.3.3 | 后端 | 长期记忆不会被自动删除 | 长期记忆 expiresAt=null，清理逻辑不影响 |
| 3.3.4 | 后端 | 用户手动添加的记忆直接为 long_term | 在记忆页手动添加 → 数据库 layer=long_term |

### 3.4 记忆注入 System Prompt

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 3.4.1 | 后端 | System Prompt 构建器正确拼接：基础 prompt + 用户偏好 + 相关记忆 | 输出字符串包含三部分内容 |
| 3.4.2 | 后端 | 无相关记忆时不注入空段落 | 无记忆时不出现"从记忆中检索到的"文字 |
| 3.4.3 | 后端 | 注入格式符合设计文档：`- [事实] xxx` / `- [偏好] xxx` | 检查输出格式 |
| 3.4.4 | 集成 | 对话中模型能利用注入的记忆信息 | 存入"用户叫小明"记忆后，问"我叫什么"模型回答正确 |

### 3.5 记忆管理 UI（MemoryScreen）

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 3.5.1 | 前端 | 记忆列表页正常加载，显示所有记忆 | 有3条记忆时列表显示3项 |
| 3.5.2 | 前端 | 每条记忆显示：内容、分类标签、层级标签、时间 | 视觉检查各字段显示 |
| 3.5.3 | 前端 | 短期记忆和长期记忆有视觉区分（不同颜色/标签） | 短期标签为橙色，长期标签为绿色（或类似区分） |
| 3.5.4 | 前端 | 分类筛选：全部/事实/偏好/事件/关系 | 选"偏好"只显示偏好类记忆 |
| 3.5.5 | 前端 | 手动添加记忆：弹出输入框，填写内容+选择分类 | 添加后列表新增一条，layer=long_term |
| 3.5.6 | 前端 | 编辑记忆：点击记忆项进入编辑，修改后保存 | 修改内容后列表更新 |
| 3.5.7 | 前端 | 删除记忆：左滑或长按弹出删除确认 | 确认后记忆从列表和数据库消失 |
| 3.5.8 | 前端 | 手动提升：短期记忆可手动升级为长期 | 操作后标签从短期变长期 |
| 3.5.9 | 前端 | 空状态：无记忆时显示引导文字"还没有记忆，对话中说'记住...'会自动保存" | 清空数据后查看 |
| 3.5.10 | 前端 | 从设置页"记忆管理"入口能正确跳转到 MemoryScreen | 点击后页面跳转正确 |

---

## 阶段四：自进化闭环（双引擎 + 偏好总结）

> 目标：Engine-B 后台总结用户偏好，偏好注入主引擎 prompt。

### 4.1 Engine-B 管理器

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.1.1 | 后端 | `SecondEngineManager` 能创建独立的 `LiteRTLMInferenceEngine` 实例 | 创建成功，Engine-B 的 state = Ready |
| 4.1.2 | 后端 | Engine-B 和 Engine-A 使用同一个模型文件，但 Conversation 完全独立 | 两个引擎的 conversation 互不影响 |
| 4.1.3 | 后端 | **互斥规则**：Engine-A 处于 Generating 状态时，Engine-B 不启动 | 模拟 Engine-A 正在生成，触发总结 → Engine-B 未启动 |
| 4.1.4 | 后端 | **抢占规则**：Engine-B 运行中用户发消息 → 立即取消 Engine-B | Engine-B 推理中发消息 → Engine-B 被 cancel，Engine-A 正常响应 |
| 4.1.5 | 后端 | Engine-B 创建失败（OOM）时不崩溃，静默降级 | 模拟内存不足，Engine-B 创建失败，应用正常运行 |
| 4.1.6 | 后端 | Engine-B 推理超时 60 秒硬中断 | 模拟超长推理，60秒后强制 cancel |
| 4.1.7 | 后端 | Engine-B 完成后正确释放资源（close conversation） | 完成后内存回落，无泄漏 |

### 4.2 触发时机

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.2.1 | 后端 | 用户 3 分钟无新消息 → 触发总结 | 发完消息等3分钟 → 日志显示"触发偏好总结" |
| 4.2.2 | 后端 | 用户切换到其他会话 → 触发当前会话的总结 | 切换会话后日志显示触发 |
| 4.2.3 | 后端 | 用户切到后台 / 锁屏 → 触发总结 | 按 Home 键后日志显示触发 |
| 4.2.4 | 后端 | 对话只有 1-2 轮时不触发（信息量不足） | 只发1条消息后等待 → 不触发 |
| 4.2.5 | 后端 | 距上次总结 < 5 分钟不触发（防频繁） | 连续触发两次 → 第二次被跳过 |
| 4.2.6 | 后端 | 总结只取最近 5 轮对话作为输入 | 有20轮对话 → prompt 中只包含最近5轮 |

### 4.3 偏好解析与存储

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.3.1 | 后端 | 正确解析 Engine-B 返回的 JSON 数组格式 | 模型返回 `[{"category":"name","content":"小明"}]` → 解析成功 |
| 4.3.2 | 后端 | JSON 解析失败时不崩溃，静默跳过 | 模型返回乱码 → 不写入数据库，无异常 |
| 4.3.3 | 后端 | 空数组 `[]` 不写入任何数据 | 模型返回 `[]` → 数据库无新增 |
| 4.3.4 | 后端 | 去重合并：同 category + 相似 content → confidence +1 | 第一次存 confidence=1，第二次同内容 → confidence=2 |
| 4.3.5 | 后端 | 新偏好（不重复）→ 新增记录 confidence=1 | 全新偏好 → 新增一条 |
| 4.3.6 | 后端 | 模型总结超时/失败 → 降级到规则提取 | Engine-B 失败 → 规则提取仍然执行 |

### 4.4 偏好注入

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.4.1 | 后端 | 只有 confidence ≥ 3 的偏好被注入 system prompt | 有 confidence=1 和 =5 的偏好各一条，prompt 中只包含 =5 的 |
| 4.4.2 | 后端 | 注入格式正确："关于当前用户的已知信息..." + 列表 | 检查 prompt 字符串格式 |
| 4.4.3 | 后端 | 无已确认偏好时不注入空段落 | confidence 都 < 3 时不出现该段落 |
| 4.4.4 | 集成 | 多轮对话后，模型回答体现用户偏好 | 存入"用户喜欢简洁回答"(confidence≥3)后，回答变短 |

### 4.5 规则提取降级

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.5.1 | 后端 | 规则提取始终生效（作为模型总结的补充） | Engine-B 正常时，规则提取也执行 |
| 4.5.2 | 后端 | 规则提取结果写入记忆表而非偏好表 | "记住我叫小明" → memories 表新增，user_preferences 表不变 |

### 4.6 设置页开关

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 4.6.1 | 前端 | 设置页"自动学习偏好"开关存在且可操作 | 开关可见，点击可切换 |
| 4.6.2 | 前端 | 关闭后 Engine-B 不再触发 | 关闭开关 → 发消息等待 → 日志无总结触发 |
| 4.6.3 | 前端 | 开关状态持久化（重启后保持） | 关闭后杀进程重启 → 开关仍为关闭 |

---

## 阶段五：技能管理

> 目标：Prompt 模板的增删改查、列表 UI、切换逻辑。

### 5.1 数据层

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 5.1.1 | 后端 | 首次安装后 skills 表自动填充4条内置技能 | 安装后查询 skills 表有4条 isBuiltIn=true |
| 5.1.2 | 后端 | 内置技能不可删除（业务层拦截） | 调用 delete 内置技能 → 不执行或抛异常 |
| 5.1.3 | 后端 | 内置技能的 systemPrompt 内容与设计文档一致 | 逐条比对 |
| 5.1.4 | 后端 | 用户自定义技能可正常增删改 | CRUD 操作验证 |
| 5.1.5 | 后端 | activate 操作：目标技能 isActive=true，其他全 false | 激活技能A → 查 isActive=true，技能B/C/D = false |
| 5.1.6 | 后端 | activate 操作：usageCount +1 | 激活后 usageCount 从5变6 |

### 5.2 技能管理 UI

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 5.2.1 | 前端 | 从设置页"角色管理"跳转到技能列表页 | 点击后正确导航 |
| 5.2.2 | 前端 | 列表分三区：当前激活 / 内置技能 / 我的技能 | 三个分区标题可见 |
| 5.2.3 | 前端 | 当前激活技能有"使用中"标签 | 激活的技能旁显示高亮标签 |
| 5.2.4 | 前端 | 每条技能显示：图标、名称、描述、使用次数 | 视觉检查各字段 |
| 5.2.5 | 前端 | 用户技能显示"编辑"和"删除"按钮 | 自定义技能行有操作按钮 |
| 5.2.6 | 前端 | 内置技能不显示"删除"按钮 | 内置技能行无删除操作 |
| 5.2.7 | 前端 | 点击技能项 → 激活该技能 | 点击后该技能变为"使用中" |
| 5.2.8 | 前端 | 添加技能：弹出表单（名称、描述、system prompt） | 填写后保存 → 列表新增一条 |
| 5.2.9 | 前端 | 编辑技能：回填已有数据，修改后保存 | 改名后列表更新 |
| 5.2.10 | 前端 | 删除技能：二次确认弹窗 | 确认后从列表消失 |
| 5.2.11 | 前端 | 空状态：用户技能为空时显示"还没有自定义技能" | 新用户看到引导 |

### 5.3 技能切换集成

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 5.3.1 | 集成 | 切换到"翻译助手"后，发英文消息 → 模型返回翻译 | 实际对话验证 prompt 生效 |
| 5.3.2 | 集成 | 切换到"代码助手"后，问编程问题 → 模型给出代码示例 | 同上 |
| 5.3.3 | 集成 | 切换技能后当前对话继续，不丢失历史 | 切换后问"刚才说了什么"能回答 |
| 5.3.4 | 集成 | 切换技能触发 Conversation 重建（新 system prompt） | 日志显示 Conversation 重建 |
| 5.3.5 | 集成 | 自定义技能的 system prompt 正确生效 | 创建"只用英文回答"技能 → 模型用英文回答 |

---

## 阶段六：全局回归测试

> 所有功能完成后的端到端验证。

### 6.1 安装与升级

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 6.1.1 | 集成 | 全新安装：数据库正确创建，内置技能预填充，无崩溃 | 清除数据后全新安装 |
| 6.1.2 | 集成 | 旧版本升级：conversations.json 自动迁移到 Room | 保留旧版数据，安装新版后数据完整 |
| 6.1.3 | 集成 | 迁移后备份文件 conversations.json.bak 存在 | 文件系统验证 |
| 6.1.4 | 集成 | 从旧版升级后所有历史对话可正常查看和继续 | 打开旧会话发新消息正常 |

### 6.2 稳定性

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 6.2.1 | 集成 | 连续使用 30 分钟无 OOM、无 ANR | 长时间对话测试 |
| 6.2.2 | 集成 | 反复切换会话 20 次无崩溃 | 快速连续切换 |
| 6.2.3 | 集成 | 后台挂起 10 分钟后恢复，应用不崩溃 | 按 Home 键等待后回来 |
| 6.2.4 | 集成 | 模型未加载时所有功能不崩溃（记忆管理、技能管理可正常操作） | 卸载模型后操作记忆页和技能页 |
| 6.2.5 | 集成 | 低端设备（4GB RAM）：单引擎正常工作，Engine-B 自动跳过 | 低内存设备测试 |

### 6.3 数据完整性

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 6.3.1 | 集成 | 杀进程后重启：最后一条消息不丢失 | 发消息后立即强杀进程，重启验证 |
| 6.3.2 | 集成 | 记忆数据不因会话操作而丢失 | 删除所有会话后记忆仍在 |
| 6.3.3 | 集成 | 技能数据不因会话操作而丢失 | 删除所有会话后技能仍在 |
| 6.3.4 | 集成 | 偏好数据不因会话操作而丢失 | 删除所有会话后偏好仍在 |

### 6.4 设置页完整性

| # | 层级 | 验收内容 | 验证方式 |
|---|------|---------|---------|
| 6.4.1 | 前端 | 设置页所有入口可点击且跳转正确 | 逐个点击验证 |
| 6.4.2 | 前端 | "角色管理" → 技能管理页 | 跳转正确 |
| 6.4.3 | 前端 | "记忆管理" → 记忆管理页 | 跳转正确 |
| 6.4.4 | 前端 | "上下文窗口大小" → 可调整保留轮数 | 修改后生效 |
| 6.4.5 | 前端 | "自动学习偏好" → 开关可切换 | 切换后 Engine-B 行为变化 |
| 6.4.6 | 前端 | 其他已有功能（深色模式、GPU设置等）不受影响 | 功能正常 |

---

## 统计

| 阶段 | 后端 | 前端 | 集成 | 合计 |
|------|------|------|------|------|
| 阶段零：Room 基础 | 14 | 0 | 0 | 14 |
| 阶段一：存储迁移 | 11 | 0 | 4 | 15 |
| 阶段二：上下文管理 | 10 | 0 | 4 | 14 |
| 阶段三：记忆系统 | 18 | 10 | 1 | 29 |
| 阶段四：自进化闭环 | 16 | 3 | 1 | 20 |
| 阶段五：技能管理 | 6 | 11 | 5 | 22 |
| 阶段六：回归测试 | 0 | 4 | 14 | 18 |
| **总计** | **75** | **28** | **29** | **132** |
