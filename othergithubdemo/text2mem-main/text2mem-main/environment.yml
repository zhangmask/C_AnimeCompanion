name: text2mem
channels:
  - conda-forge
dependencies:
  # Python environment
  - python>=3.8
  - pip

  # Core dependencies
  - pydantic>=2.7.0
  - jsonschema>=4.21.0
  - sqlalchemy>=2.0.0
  - httpx>=0.25.0
  - numpy>=1.21.0

  # Development tools
  - pytest>=7.0.0
  - black>=23.0.0
  - isort>=5.0.0
  - mypy>=1.0.0
  - jupyter>=1.0.0

  # Visualization
  - matplotlib>=3.5.0
  - seaborn>=0.11.0
  - plotly>=5.0.0

  # Packages installed via pip
  - pip:
    - orjson>=3.9.0
    - sqlite-utils>=3.36.0
    - openai>=1.6.0
    - -e .  # Install current project in editable mode
