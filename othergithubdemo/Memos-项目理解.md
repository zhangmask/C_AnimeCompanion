# Memos 项目理解

**非 Agent 记忆系统**。这是人类用的笔记工具（Go + React），没有 LLM 自动提取、没有向量索引、没有记忆 Schema。与当前研究主题无关。

**技术栈**: Go 单二进制 | SQLite/MySQL/PostgreSQL | REST + gRPC API

**部署**: `docker run -p 5230:5230 neosmemo/memos:stable`
